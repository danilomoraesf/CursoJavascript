

function paginaProdutos(){
    const header = document.createElement('header'); // criando header
    const h1 = document.createElement('h1'); // criando header
    h1.innerHTML = "Lista de Produtos"; // manipulando texto
    header.appendChild(h1); // inserindo título no menu (header)
    const main = document.createElement('main'); // onde ficarão as fotos
   

    















} 






body.addEventListener("load", paginaProdutos());